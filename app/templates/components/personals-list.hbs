<h3 {{action 'showListings'}}>Personals</h3>
{{#if showListings}}
  <ul>
    {{#each model as |listing|}}
      {{#if (eq listing.type "personals")}}
        <li>
          {{#link-to 'listing' listing.id}}
            {{listing.author}} is looking for {{listing.title}}
          {{/link-to}}
        </li>
      {{/if}}
    {{/each}}
  </ul>
{{/if}}
